@model StateProvinceModel

@{
    //page title
    ViewBag.Title = T("Admin.Configuration.Countries.States.EditStateDetails").Text;
    Html.SetActiveMenuItemSystemName("Countries");
}

<form asp-controller="Country" asp-action="StateEdit"
      asp-route-btnId="@Context.Request.Query["btnId"]"
      asp-route-formId="@Context.Request.Query["formId"]">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Configuration.Countries.States.EditStateDetails")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a href="@Url.Content("~/Admin/Country/StateList?countryId="+Model.CountryId)">@T("Admin.Configuration.States.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateState", Model)
</form>