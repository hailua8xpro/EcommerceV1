@model DistrictModel

@{
    ViewBag.Title = T("Admin.Configuration.Countries.District.AddNew").Text;
    Html.SetActiveMenuItemSystemName("Countries");
}

<form asp-controller="Country" asp-action="DistrictCreate"
      asp-route-stateProvinceId="@Context.Request.Query["stateProvinceId"]"
      asp-route-btnId="@Context.Request.Query["btnId"]"
      asp-route-formId="@Context.Request.Query["formId"]">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Configuration.Countries.Districts.AddNew")
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateDistrict", Model)
</form>